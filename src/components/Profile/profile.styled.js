import styled from 'styled-components';

const ProfileWrapper = styled.button`
  background: none;
  border: none;
  color: inherit;
  cursor: pointer;
  display: flex;
  font: inherit;
  margin-left: auto;
  outline: inherit;
  padding: 0;
`;

export { ProfileWrapper };
